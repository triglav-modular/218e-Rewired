name: Windows flasher

# The .bat has never run on Windows.  A runner cannot flash anything — there is
# no instrument — but it can exercise everything up to the point one is needed,
# which is where the mechanical risk lives: certutil's checksum output, the
# delayed-expansion loops, label resolution, and whether the bundled binaries
# run on a clean machine at all.
#
# With no 218e present the script exits at the MIDI check, which is before the
# Zadig launch.  That matters: START /WAIT on a headless runner would hang.

on:
  push:
    paths:
      - '218e_Rewired_Flasher.bat'
      - 'windows/support/**'
      - 'windows/launcher/**'
      # The flasher shells out to these, so a change to one can break it
      # without the .bat itself being touched.  Named as a group rather than
      # one by one: a new helper was added and not listed, and the run that
      # would have tested it never started.
      - 'tools/*.ps1'
      - 'tools/validate_hex.py'
      # The workflow runs these too; a fixture added without a run against
      # the PowerShell validator is the documented drift hole reopened.
      - 'tools/hex_fixtures.py'
      - 'tools/check_scan_output.py'
      - 'tools/check-bat-vars.py'
      - '.github/workflows/windows-flasher.yml'
  workflow_dispatch:

jobs:
  flasher:
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7

      - name: No variable is read without being set
        shell: bash
        run: python3 tools/check-bat-vars.py

      - name: The bundled tools run at all
        shell: cmd
        run: |
          echo === dfu-programmer ===
          windows\support\dfu-programmer.exe at32uc3b1256 get bootloader-version
          echo exit=%ERRORLEVEL%  ^("no device present" is the pass; 1.1.0
          echo exits 3 where 1.0.0 exited 1, which is why the flasher reads
          echo the text and only treats a nonzero exit as corroborating^)
          echo === sendmidi ===
          windows\support\sendmidi.exe list
          echo exit=%ERRORLEVEL%

      - name: The banana renders with its eyes
        shell: pwsh
        run: |
          # cmd treats ^ as an escape, so "^ " prints a bare space and single
          # carets would leave the banana blind.  Run the real :banana block
          # through cmd and check the eyes survive to stdout.
          $src   = Get-Content 218e_Rewired_Flasher.bat
          $start = ($src | Select-String -Pattern '^:banana$').LineNumber
          if (-not $start) { throw ":banana label not found" }
          $body = @()
          for ($i = $start; $i -lt $src.Count; $i++) {
            if ($src[$i] -match '^EXIT /B 0') { break }
            if ($src[$i] -match '^ECHO ')     { $body += $src[$i] }
          }
          if ($body.Count -lt 20) { throw "banana is $($body.Count) lines, expected 22" }
          Set-Content -Path banana.bat -Value (@('@echo off') + $body) -Encoding Ascii
          $out = & cmd /c banana.bat
          $out | ForEach-Object { Write-Host $_ }
          $eyes = ($out | Where-Object { $_ -match '\^' }).Count
          if ($eyes -lt 1) { throw "the banana printed no caret: the eyes were eaten" }
          Write-Host "banana: $($body.Count) lines, $eyes line(s) carrying eyes"

      - name: certutil checksum parsing matches the real hash
        shell: cmd
        run: |
          echo hello> probe.bin
          for /f "skip=1 tokens=* delims=" %%H in ('certutil -hashfile probe.bin SHA256 ^| findstr /R "^[0-9a-f ]*$"') do (
            if not defined SHA set "SHA=%%H"
          )
          setlocal EnableDelayedExpansion
          set "SHA=!SHA: =!"
          echo parsed: [!SHA!]
          if "!SHA!"=="" ( echo FAILED: parsed nothing & exit /b 1 )
          endlocal

      - name: No image present - must refuse without touching anything
        shell: cmd
        run: |
          (echo 1& echo YES)> answers0.txt
          CALL 218e_Rewired_Flasher.bat < answers0.txt > out.txt 2>&1
          type out.txt
          findstr /C:"Looked in:" out.txt >NUL || (echo FAILED: did not say where it looked & exit /b 1)
          findstr /C:"No flashable 218e V3 image" out.txt >NUL || (echo FAILED: wrong message & exit /b 1)
          findstr /C:"Erasing the application flash" out.txt >NUL && (echo FAILED: erased with no image & exit /b 1)
          echo PASS: refused, and said where it looked
          exit /b 0

      - name: The launcher runs the flasher and carries the banana
        shell: cmd
        run: |
          rem Not shipped yet - unsigned it trips Defender's dropper
          rem heuristics - but kept working for the day it is signed.  The
          rem launcher looks for the .bat beside itself, so a copy at the
          rem repository root sits exactly where a download would put it.
          rem With no image present the flasher refuses - reaching that
          rem message proves the whole chain: exe, cmd, script.
          copy "windows\launcher\218e Rewired Flasher.exe" launcher_copy.exe >NUL
          (echo 1& echo YES)> ansl.txt
          launcher_copy.exe < ansl.txt > outl.txt 2>&1
          type outl.txt
          findstr /C:"No flashable 218e V3 image" outl.txt >NUL || (echo FAILED: the launcher did not reach the flasher & exit /b 1)
          rem The icon has to be the launcher's own.  ExtractAssociatedIcon
          rem falls back to the stock exe icon, so ask the resource table
          rem instead: a group-icon resource with id 1.
          powershell -NoProfile -Command "Add-Type -Namespace W -Name R -MemberDefinition '[DllImport(\"kernel32.dll\", SetLastError = true, CharSet = CharSet.Unicode)] public static extern IntPtr LoadLibraryExW(string path, IntPtr file, uint flags); [DllImport(\"kernel32.dll\", SetLastError = true)] public static extern IntPtr FindResourceW(IntPtr module, IntPtr name, IntPtr type);'; $m = [W.R]::LoadLibraryExW((Resolve-Path 'launcher_copy.exe'), [IntPtr]::Zero, 2); if ($m -eq [IntPtr]::Zero) { exit 1 }; if ([W.R]::FindResourceW($m, [IntPtr]1, [IntPtr]14) -eq [IntPtr]::Zero) { exit 1 }" || (echo FAILED: no icon resource in the launcher & exit /b 1)
          del launcher_copy.exe ansl.txt outl.txt 2>NUL
          echo PASS: the launcher chains to the flasher and has its own icon
          exit /b 0

      - name: The hex validator accepts good images and rejects bad
        shell: cmd
        run: |
          pwsh -File tools\Make-TestImage.ps1 -Path good.hex
          python tools\validate_hex.py good.hex || (echo FAILED: rejected a valid image & exit /b 1)
          echo not a hex file> bad.hex
          python tools\validate_hex.py bad.hex && (echo FAILED: accepted junk & exit /b 1)
          del /Q good.hex bad.hex 2>NUL
          echo PASS: validator discriminates
          exit /b 0

      - name: The PowerShell scanner refuses what dfu-programmer refuses
        shell: cmd
        run: |
          rem The same fixtures the other two validators are held to.  The list
          rem is whatever hex_fixtures.py wrote, not names repeated here: three
          rem fixtures went unchecked on this side once because they were.
          python tools\hex_fixtures.py fixtures || (echo FAILED: could not write fixtures & exit /b 1)
          powershell -NoProfile -ExecutionPolicy Bypass -File tools\Scan-Images.ps1 -DirList "%CD%\fixtures" -Explain > scan.txt 2> scanerr.txt
          type scan.txt
          type scanerr.txt
          python tools\check_scan_output.py fixtures scan.txt --python-too
          if errorlevel 1 exit /b 1
          rmdir /S /Q fixtures 2>NUL
          del /Q scan.txt scanerr.txt 2>NUL
          exit /b 0

      - name: The menu helper parses, and stands aside without a keyboard
        shell: cmd
        run: |
          rem A syntax error here would only show up when someone opened the
          rem flasher, since batch cannot see a PowerShell parse failure coming.
          powershell -NoProfile -Command "$e=$null;[void][Management.Automation.Language.Parser]::ParseFile('tools\Show-Menu.ps1',[ref]$null,[ref]$e); if($e.Count){$e|ForEach-Object{Write-Host $_}; exit 1}" || (echo FAILED: Show-Menu.ps1 does not parse & exit /b 1)
          powershell -NoProfile -Command "$e=$null;[void][Management.Automation.Language.Parser]::ParseFile('tools\Test-ArrowMenu.ps1',[ref]$null,[ref]$e); if($e.Count){$e|ForEach-Object{Write-Host $_}; exit 1}" || (echo FAILED: Test-ArrowMenu.ps1 does not parse & exit /b 1)
          echo PASS: Show-Menu.ps1 parses

      - name: The arrow menu is actually driveable
        shell: cmd
        run: |
          rem The one thing no other step here can see.  Every step's output is
          rem captured, and the helper stands aside when its output is captured,
          rem so the arrow menu is invisible to ordinary CI - which is how it
          rem shipped twice unable to read a key at all.  This gives itself a
          rem console, types into it, and reads the answer out of the file.
          powershell -NoProfile -ExecutionPolicy Bypass -File tools\Test-ArrowMenu.ps1 -Report menuout.txt
          set RC=%ERRORLEVEL%
          if exist menuout.txt type menuout.txt
          if not "%RC%"=="0" (echo FAILED: the arrow menu could not be driven & exit /b 1)
          del menuout.txt 2>NUL
          rem With no console to read arrow keys from it must write no answer
          rem at all, so the batch knows to ask in its own way.  It must not
          rem try to read the pipe: cmd buffers redirected input and hands a
          rem child process none of it, which is how this went wrong.  This
          rem check sat below an exit /b 0 for a while, silently dead - keep
          rem it above the exit.
          > menu.txt echo Flash firmware onto the 218e
          >> menu.txt echo Erases the chip and writes a new image.
          >> menu.txt echo --
          >> menu.txt echo Get the keyboard out of DFU mode
          >> menu.txt echo Sends START. Flashes nothing, erases nothing.
          if exist pick.txt del /Q pick.txt
          echo 2| powershell -NoProfile -ExecutionPolicy Bypass -File tools\Show-Menu.ps1 -Path menu.txt -Out pick.txt -Title "What would you like to do?"
          if exist pick.txt (echo FAILED: wrote an answer it could not have read & exit /b 1)
          echo PASS: stands aside when there is no keyboard
          del /Q menu.txt 2>NUL
          exit /b 0

      - name: More than one image - must list them and ask
        shell: cmd
        run: |
          del /Q *.hex 2>NUL
          del /Q firmware\*.hex 2>NUL
          rem Both in the firmware folder: the flasher looks there and nowhere
          rem else now, so an image in Downloads is no longer its business.
          mkdir firmware 2>NUL
          pwsh -File tools\Make-TestImage.ps1 -Path firmware\custom.hex
          pwsh -File tools\Make-TestImage.ps1 -Path firmware\second.hex -Bytes 20480
          rem Flash, consent, then pick nothing: it must have listed both and stopped.
          (echo 1& echo YES& echo.& echo.)> ans2.txt
          CALL 218e_Rewired_Flasher.bat < ans2.txt > out4.txt 2>&1
          type out4.txt
          findstr /C:"flashable images found" out4.txt >NUL || (echo FAILED: did not list the images & exit /b 1)
          findstr /C:"Newest first" out4.txt >NUL || (echo FAILED: no ordering shown & exit /b 1)
          findstr /C:"Nothing chosen" out4.txt >NUL || (echo FAILED: did not stop on an empty choice & exit /b 1)
          findstr /C:"Erasing the application flash" out4.txt >NUL && (echo FAILED: erased without a choice & exit /b 1)
          del firmware\second.hex firmware\custom.hex 2>NUL
          echo PASS: listed, ordered, stopped without a choice
          exit /b 0

      - name: An image outside the package is not picked up
        shell: cmd
        run: |
          del /Q firmware\*.hex 2>NUL
          mkdir "%USERPROFILE%\Downloads" 2>NUL
          pwsh -File tools\Make-TestImage.ps1 -Path "%USERPROFILE%\Downloads\stray.hex"
          mkdir firmware 2>NUL
          pwsh -File tools\Make-TestImage.ps1 -Path firmware\mine.hex
          (echo 1& echo YES& echo.)> ans5.txt
          CALL 218e_Rewired_Flasher.bat < ans5.txt > out5.txt 2>&1
          type out5.txt
          findstr /C:"stray.hex" out5.txt >NUL && (echo FAILED: listed an image from Downloads & exit /b 1)
          findstr /C:"mine.hex" out5.txt >NUL || (echo FAILED: did not list the package image & exit /b 1)
          del "%USERPROFILE%\Downloads\stray.hex" firmware\mine.hex 2>NUL
          echo PASS: only the package folder is searched
          exit /b 0

      - name: A package folder with brackets in its name
        shell: cmd
        run: |
          rem Windows names a second download "Rewired-Windows (1)".  cmd
          rem expands %VAR% while it parses a block, so a closing bracket in
          rem the value ends the block early: the path printed as far as the
          rem bracket and both halves of an IF/ELSE ran.
          set "PKG=%TEMP%\Rewired-Windows (1)"
          rmdir /S /Q "%PKG%" 2>NUL
          mkdir "%PKG%\firmware" "%PKG%\tools" 2>NUL
          copy /Y 218e_Rewired_Flasher.bat "%PKG%\" >NUL
          copy /Y tools\*.ps1 "%PKG%\tools\" >NUL
          copy /Y tools\validate_hex.py "%PKG%\tools\" >NUL
          copy /Y windows\support\*.exe "%PKG%\tools\" >NUL
          pwsh -File tools\Make-TestImage.ps1 -Path "%PKG%\firmware\one.hex"
          (echo 1& echo YES& echo.)> ansb.txt
          CALL "%PKG%\218e_Rewired_Flasher.bat" < ansb.txt > out6.txt 2>&1
          type out6.txt
          findstr /C:"no such folder" out6.txt >NUL && (echo FAILED: block ended at the bracket & exit /b 1)
          findstr /C:"no firmware folder" out6.txt >NUL && (echo FAILED: did not see its own firmware folder & exit /b 1)
          findstr /C:"one.hex" out6.txt >NUL || (echo FAILED: did not find the image & exit /b 1)
          rmdir /S /Q "%PKG%" 2>NUL
          echo PASS: brackets in the package path are survivable
          exit /b 0

      - name: A lone custom image flashes without extra ceremony
        shell: cmd
        run: |
          rem The only valid image present is NOT the one shipped with the
          rem package.  It should be selected and used, not gated behind a
          rem second confirmation - the flasher installs any valid image.
          rem Clear anything an earlier step left where the flasher searches,
          rem so "exactly one image" is true rather than assumed.
          del /Q *.hex 2>NUL
          del /Q firmware\*.hex 2>NUL
          mkdir firmware 2>NUL
          pwsh -File tools\Make-TestImage.ps1 -Path firmware\custom.hex
          (echo 1& echo YES& echo.)> ans.txt
          CALL 218e_Rewired_Flasher.bat < ans.txt > out3.txt 2>&1
          type out3.txt
          findstr /C:"Type FLASH" out3.txt >NUL && (echo FAILED: still gating on the checksum & exit /b 1)
          findstr /C:"is not the image this flasher" out3.txt >NUL && (echo FAILED: pairing language remains & exit /b 1)
          findstr /C:"image " out3.txt >NUL || (echo FAILED: did not report the image fingerprint & exit /b 1)
          del firmware\custom.hex 2>NUL
          echo PASS: a custom image is used without a checksum gate
          exit /b 0

      - name: Fuse values parse out of dfu-programmer's format
        shell: cmd
        run: |
          rem Reaching this needs a real device, so the parsing idiom is tested
          rem directly against the exact lines dfu-programmer prints.  The
          rem format string is "%s%s0x%02x (%d)\n" and is byte-identical in
          rem 1.0.0 and 1.1.0, so moving Windows to our own 1.1.0 build did
          rem not disturb the delims=() idiom the fuse gate depends on.
          setlocal EnableDelayedExpansion
          set "FAIL=0"
          for %%P in ("Bootloader protected area: 0x03 (3)=3" "ISP Force: 0x01 (1)=1" "Bootloader Version: 0x02 (2)=2") do (
            for /f "tokens=1,2 delims==" %%A in (%%P) do (
              set "V="
              for /f "tokens=2 delims=()" %%V in ("%%A") do set "V=%%V"
              if not "!V!"=="%%B" (echo FAILED: %%A parsed as [!V!], expected %%B & set "FAIL=1")
            )
          )
          rem A line with no brackets must leave the value unset, not garbage.
          set "V=untouched"
          for /f "tokens=2 delims=()" %%V in ("dfu-programmer: no device present.") do set "V=%%V"
          if not "!V!"=="untouched" (echo FAILED: bracketless line set [!V!] & set "FAIL=1")
          if "!FAIL!"=="1" exit /b 1
          echo PASS: fuse values parse correctly
          exit /b 0

      - name: The USB probe runs and reports no Atmel DFU device
        shell: cmd
        run: |
          rem No instrument on a runner, so it must say ABSENT rather than fail.
          for /f "delims=" %%S in ('powershell -NoProfile -ExecutionPolicy Bypass -File tools\Find-DfuDevice.ps1 2^>NUL') do set "STATE=%%S"
          echo probe said: [%STATE%]
          if not "%STATE%"=="ABSENT" (echo FAILED: expected ABSENT with no device & exit /b 1)
          echo PASS: USB probe reports absence cleanly
          exit /b 0

      - name: With a matching image - must verify and reach the MIDI step
        shell: cmd
        run: |
          rem Stand-in image: patch a copy of the flasher to expect its hash,
          rem so the checksum path runs for real without shipping firmware.
          rem It has to be a genuine app-region image now - the flasher
          rem validates structure before it ever looks at a checksum.
          del /Q *.hex 2>NUL
          del /Q "%USERPROFILE%\Downloads\*.hex" "%USERPROFILE%\Desktop\*.hex" 2>NUL
          mkdir firmware 2>NUL
          del /Q firmware\*.hex 2>NUL
          pwsh -File tools\Make-TestImage.ps1 -Path firmware\218eV3_v369_Rewired_DFU.hex
          for /f "skip=1 tokens=* delims=" %%H in ('certutil -hashfile firmware\218eV3_v369_Rewired_DFU.hex SHA256 ^| findstr /R "^[0-9a-f ]*$"') do (
            if not defined SHA set "SHA=%%H"
          )
          setlocal EnableDelayedExpansion
          set "SHA=!SHA: =!"
          powershell -Command "(Get-Content '218e_Rewired_Flasher.bat' -Raw) -replace '(?<=EXPECTED_SHA256=)[0-9a-f]{64}', '!SHA!' | Set-Content 'ci.bat'"
          endlocal
          rem Answers come from a file, not a pipe: a piped line is consumed by
          rem the first subshell the script spawns, so SET /P then reads EOF and
          rem the consent gate rejects an empty answer.  Repeated so each prompt
          rem up to the MIDI check has one.
          (echo 1& echo YES& echo.& echo.)> answers.txt
          rem CALL, not a bare invocation: running one .bat from another without
          rem CALL transfers control and never returns, so every assertion below
          rem would be skipped and the step would report the flasher's own exit
          rem code instead.
          CALL ci.bat < answers.txt > out2.txt 2>&1
          type out2.txt
          rem A pattern, not a literal version: asserting "1.0.0" here meant
          rem every version bump broke this step and nothing else.
          findstr /R /C:"Rewired [0-9]*\.[0-9]*\.[0-9]*" out2.txt >NUL || (echo FAILED: version not shown & exit /b 1)
          findstr /C:"No MIDI output port" out2.txt >NUL || (echo FAILED: did not stop at MIDI & exit /b 1)
          rem The runner has no MIDI at all, so the list must be shown and empty.
          findstr /C:"MIDI output ports Windows is offering" out2.txt >NUL || (echo FAILED: did not show the port list & exit /b 1)
          rem Not the bare word "erase" - the warning text contains "was erased".
          findstr /C:"Erasing the application flash" out2.txt >NUL && (echo FAILED: reached the erase & exit /b 1)
          rem "Starting Zadig", not the bare word: the MIDI diagnostics mention
          rem Zadig by name when explaining how to undo a wrong driver binding.
          findstr /C:"Starting Zadig" out2.txt >NUL && (echo FAILED: reached the Zadig launch & exit /b 1)
          echo PASS: verified, versioned, stopped before touching anything
          rem ECHO does not reset ERRORLEVEL in cmd, so without this the step
          rem inherits the 1 from the last findstr that correctly found nothing.
          exit /b 0
